# Grand Publicador

**Grand Publicador** — современная платформа для управления контентом и публикациями в социальных сетях, реализованная в формате Telegram Mini App. Система позволяет создавать проекты, управлять каналами в различных социальных сетях, планировать и автоматизировать публикации.

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологический стек](#-технологический-стек)
- [Быстрый старт](#-быстрый-старт)
- [Установка и настройка](#-установка-и-настройка)
- [Запуск в режиме разработки](#-запуск-в-режиме-разработки)
- [Продакшн развертывание](#-продакшн-развертывание)
- [Конфигурация](#-конфигурация)
- [Структура проекта](#-структура-проекта)
- [Принцип работы](#-принцип-работы)
- [API документация](#-api-документация)
- [Тестирование](#-тестирование)
- [Разработка](#-разработка)

## ✨ Возможности

### Управление проектами
- Создание и управление множественными проектами
- Система ролей и прав доступа (Owner, Admin, Editor, Viewer)
- Приглашение участников в проекты
- Архивирование проектов с виртуальным каскадным удалением

### Управление каналами
- Поддержка множественных социальных сетей (Telegram, Instagram, VK, YouTube, TikTok, X, Facebook, LinkedIn, собственные сайты)
- Привязка каналов к проектам
- Управление учетными данными для каждого канала
- Активация/деактивация каналов

### Публикации и посты
- Создание публикаций с различными типами контента (Post, Article, News, Video, Short, Story)
- Планирование публикаций с указанием даты и времени
- Автоматическая публикация по расписанию
- Поддержка медиафайлов и тегов
- Статусы публикаций (Draft, Scheduled, Published, Failed, Expired)
- Связь публикаций с несколькими каналами

### Система архивирования
- Мягкое удаление с возможностью восстановления
- Виртуальное каскадное архивирование связанных сущностей
- Перемещение сущностей между проектами
- Окончательное удаление архивированных элементов

### Безопасность и аутентификация
- Авторизация через Telegram (проверка initData)
- JWT токены для фронтенд приложения
- API токены для внешних интеграций
- Scope-based доступ для API токенов (ограничение по проектам)
- Защита всех эндпоинтов

## 🛠 Технологический стек

### Backend
- **NestJS 11** — модульный фреймворк для Node.js
- **Fastify** — высокопроизводительный веб-сервер
- **Prisma 6** — современный ORM с типобезопасностью
- **SQLite** — встроенная база данных
- **JWT** — аутентификация и авторизация
- **Passport** — стратегии аутентификации
- **class-validator** — валидация DTO
- **Pino** — структурированное логирование
- **js-yaml** — работа с конфигурацией

### Frontend
- **Nuxt 4** — Vue.js фреймворк с SSR поддержкой (в режиме SPA)
- **Vue 3** — прогрессивный JavaScript фреймворк
- **Nuxt UI 4** — компонентная библиотека на базе Tailwind CSS
- **Pinia** — управление состоянием
- **TipTap** — rich-text редактор
- **@tma.js/sdk-vue** — интеграция с Telegram Mini Apps
- **@nuxtjs/i18n** — интернационализация
- **Tailwind CSS 4** — utility-first CSS фреймворк

### DevOps
- **Docker** — контейнеризация
- **pnpm** — быстрый пакетный менеджер
- **Jest** — тестирование
- **ESLint** — линтинг кода
- **Prettier** — форматирование кода

## 🚀 Быстрый старт

### Требования
- **Node.js** >= 22.0.0
- **pnpm** >= 10.13.1
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/BotFather))

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd grand-publicador

# Установка зависимостей бэкенда
pnpm install

# Установка зависимостей фронтенда
cd ui && pnpm install && cd ..
```

## ⚙️ Установка и настройка

### 1. Настройка переменных окружения

Для разработки:
```bash
cp .env.development.example .env
```

Для продакшн:
```bash
cp .env.production.example .env
```

Отредактируйте `.env` файл:

```env
# Основные настройки приложения
NODE_ENV=development
SERVER_HOST=localhost
SERVER_PORT=8080
LOG_LEVEL=debug
TZ=UTC

# Директория для данных (БД, конфиг, логи)
DATA_DIR="./test-data"

# ID главного администратора в Telegram (опционально)
TELEGRAM_ADMIN_ID="123456789"

# ОБЯЗАТЕЛЬНО: Секретный ключ для JWT (сгенерируйте длинную случайную строку)
JWT_SECRET="your-very-long-and-secure-secret-key-here"

# ОБЯЗАТЕЛЬНО: Токен бота от @BotFather
TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
```

### 2. Инициализация базы данных

**Важно:** Используйте `prisma-wrapper.mjs` для всех Prisma команд. Этот скрипт автоматически устанавливает `DATABASE_URL` на основе `DATA_DIR`.

```bash
# Создание миграций и базы данных
node prisma-wrapper.mjs migrate dev --name init

# Или только применение миграций (для продакшн)
node prisma-wrapper.mjs migrate deploy

# Генерация Prisma клиента
npx prisma generate

# Наполнение базы тестовыми данными (опционально)
node prisma-wrapper.mjs db seed
``` 

### 3. Настройка Telegram Mini App

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота и добавьте его в `.env`
3. Настройте Mini App через [@BotFather](https://t.me/BotFather):
   - Команда: `/newapp`
   - Укажите URL вашего приложения (для разработки: `http://localhost:3000`)
   - Загрузите иконку и описание

## 💻 Запуск в режиме разработки

### Backend

```bash
# Запуск с hot-reload
pnpm start:dev

# Запуск с отладкой
pnpm start:debug
```

Сервер будет доступен на `http://localhost:8080`

API эндпоинты: `http://localhost:8080/api/v1/*`

### Frontend

```bash
cd ui
pnpm dev
```

Интерфейс будет доступен на `http://localhost:3000`

### Режим разработки без Telegram

Для тестирования без Telegram можно использовать dev-режим:

```env
# В .env файле
VITE_DEV_MODE=true
VITE_DEV_TELEGRAM_ID=123456789
VITE_DEV_USERNAME=testuser
VITE_DEV_FIRST_NAME=Test
VITE_DEV_LAST_NAME=User
```

При этом проверка Telegram initData будет пропущена.

## 🚢 Продакшн развертывание

### Вариант 1: Docker (рекомендуется)

Подробное руководство: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md)

```bash
cd docker

# Отредактируйте docker-compose.yml
# Установите JWT_SECRET и TELEGRAM_BOT_TOKEN

# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

Приложение будет доступно на `http://localhost:8080`

### Вариант 2: Ручная сборка

**Backend:**
```bash
# Сборка
pnpm build

# Применение миграций
node prisma-wrapper.mjs migrate deploy

# Запуск
pnpm start:prod
```

**Frontend:**
```bash
cd ui

# Сборка
pnpm build

# Статические файлы будут в ui/.output/public
# Они автоматически раздаются бэкендом через Fastify Static
```

### Переменные окружения для продакшн

```env
NODE_ENV=production
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
DATA_DIR="/app/data"
JWT_SECRET="your-production-secret"
TELEGRAM_BOT_TOKEN="your-bot-token"
TELEGRAM_ADMIN_ID="your-telegram-id"
LOG_LEVEL=info
```

## 🔧 Конфигурация

### Система конфигурации

Приложение использует **YAML-файл** для хранения настроек: `DATA_DIR/app-config.yaml`

#### Особенности:
- **Hot Reload** — изменения применяются без перезапуска сервера
- **Env Substitution** — использование `${VAR_NAME}` для подстановки переменных окружения
- **UI редактор** — изменение настроек через админ-панель
- **Валидация** — автоматическая проверка корректности конфигурации

#### Структура конфигурации:

```yaml
server:
  host: ${SERVER_HOST}
  port: ${SERVER_PORT}
  basePath: ''

auth:
  jwt:
    secret: ${JWT_SECRET}
    expiresIn: '7d'
  telegram:
    botToken: ${TELEGRAM_BOT_TOKEN}

telegram:
  adminId: ${TELEGRAM_ADMIN_ID}

logging:
  level: info
  pretty: false
```

Подробнее: [docs/CONFIGURATION.md](docs/CONFIGURATION.md)

### Переменные окружения

| Переменная | Обязательная | Описание | Пример |
|------------|--------------|----------|--------|
| `NODE_ENV` | Нет | Режим работы | `development`, `production` |
| `SERVER_HOST` | Нет | Хост сервера | `localhost`, `0.0.0.0` |
| `SERVER_PORT` | Нет | Порт сервера | `8080` |
| `DATA_DIR` | Да | Директория для данных | `./test-data`, `/app/data` |
| `JWT_SECRET` | Да | Секрет для JWT | Длинная случайная строка |
| `TELEGRAM_BOT_TOKEN` | Да | Токен Telegram бота | `1234567890:ABC...` |
| `TELEGRAM_ADMIN_ID` | Нет | ID главного администратора | `123456789` |
| `LOG_LEVEL` | Нет | Уровень логирования | `debug`, `info`, `warn`, `error` |
| `TZ` | Нет | Часовой пояс | `UTC`, `Europe/Moscow` |
| `VITE_DEV_MODE` | Нет | Режим разработки без Telegram | `true`, `false` |
| `VITE_DEV_TELEGRAM_ID` | Нет | Mock Telegram ID для dev | `123456789` |

## 🏗 Структура проекта

```
grand-publicador/
├── src/                          # Backend (NestJS)
│   ├── common/                   # Общие утилиты, guards, decorators
│   ├── config/                   # Конфигурация приложения
│   ├── modules/                  # Бизнес-модули
│   │   ├── api-tokens/           # Управление API токенами
│   │   ├── archive/              # Система архивирования
│   │   ├── auth/                 # Аутентификация
│   │   ├── channels/             # Управление каналами
│   │   ├── posts/                # Управление постами
│   │   ├── projects/             # Управление проектами
│   │   ├── publications/         # Управление публикациями
│   │   └── users/                # Управление пользователями
│   ├── app.module.ts             # Главный модуль
│   └── main.ts                   # Точка входа
│
├── ui/                           # Frontend (Nuxt 4)
│   ├── app/
│   │   ├── components/           # Vue компоненты
│   │   ├── composables/          # Переиспользуемая логика
│   │   ├── pages/                # Страницы (роутинг)
│   │   ├── stores/               # Pinia stores
│   │   └── utils/                # Утилиты
│   └── nuxt.config.ts
│
├── prisma/                       # База данных
│   ├── migrations/               # Миграции
│   ├── schema.prisma             # Схема БД
│   └── seed.ts                   # Тестовые данные
│
├── test/                         # Тесты
│   ├── unit/                     # Unit тесты
│   └── e2e/                      # E2E тесты
│
├── docker/                       # Docker конфигурация
│
└── docs/                         # Документация
    ├── API.md                    # API документация
    ├── ARCHITECTURE.md           # Архитектура проекта
    ├── CONFIGURATION.md          # Конфигурация
    └── DEPLOYMENT.md             # Развертывание
```

**Подробная архитектура:** [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

## 🔄 Принцип работы

### 1. Аутентификация и авторизация

#### Для пользователей (Telegram Mini App):
1. Пользователь открывает Mini App в Telegram
2. Telegram SDK предоставляет `initData` с информацией о пользователе
3. Frontend отправляет `initData` на `/api/v1/auth/telegram`
4. Backend проверяет подпись Telegram и создает/обновляет пользователя
5. Возвращается JWT токен, который сохраняется в cookies
6. Все последующие запросы используют этот токен

#### Для внешних интеграций (API токены):
1. Пользователь создает API токен через UI (Настройки → API Tokens)
2. Указывает название и scope (доступные проекты)
3. Получает токен (показывается один раз!)
4. Использует токен в заголовке `x-api-key` или `Authorization: Bearer`

### 2. Управление проектами

```
Проект (Project)
  ├── Владелец (Owner)
  ├── Участники (Members) с ролями:
  │   ├── OWNER - полный доступ
  │   ├── ADMIN - управление участниками и каналами
  │   ├── EDITOR - создание и редактирование контента
  │   └── VIEWER - только просмотр
  ├── Каналы (Channels)
  │   └── Посты (Posts)
  └── Публикации (Publications)
      └── Посты (Posts)
```

### 3. Публикация контента

#### Вариант 1: Прямая публикация
1. Создается пост напрямую для канала
2. Указывается контент, медиафайлы, дата публикации
3. Устанавливается статус `SCHEDULED`
4. Пост ожидает публикации в соответствии с расписанием

#### Вариант 2: Через публикацию
1. Создается публикация (Publication) - шаблон контента
2. Публикация связывается с несколькими каналами
3. Для каждого канала создается отдельный пост
4. Посты публикуются по расписанию

### 4. Система архивирования

- **Мягкое удаление**: устанавливается `archived_at` и `archived_by`
- **Виртуальное каскадирование**: при архивации проекта архивируются все связанные каналы, публикации и посты
- **Восстановление**: можно восстановить архивированные элементы
- **Окончательное удаление**: физическое удаление из БД

### 6. Безопасность

#### Защита эндпоинтов:
- Все API эндпоинты защищены `JwtOrApiTokenGuard`
- Проверка прав доступа на уровне сервисов
- Валидация всех входных данных через DTO

#### API токены:
- Хранятся в зашифрованном виде
- Поддержка scope (ограничение по проектам)
- Отслеживание последнего использования
- Возможность отзыва в любой момент

## 📚 API документация

Подробная документация API доступна в файле [docs/API.md](docs/API.md)

### Базовый URL
- Development: `http://localhost:8080/api/v1`
- Production: `https://your-domain.com/api/v1`

### Аутентификация

Все эндпоинты (кроме `/auth/telegram`) требуют аутентификации:

**JWT токен (для фронтенда):**
```
Authorization: Bearer <JWT_TOKEN>
```

**API токен (для внешних интеграций):**
```
x-api-key: <API_TOKEN>
```
или
```
Authorization: Bearer <API_TOKEN>
```

### Основные эндпоинты

- **Auth**: `/api/v1/auth/*` - Аутентификация
- **Projects**: `/api/v1/projects/*` - Управление проектами
- **Channels**: `/api/v1/channels/*` - Управление каналами
- **Publications**: `/api/v1/publications/*` - Управление публикациями
- **Posts**: `/api/v1/posts/*` - Управление постами
- **API Tokens**: `/api/v1/api-tokens/*` - Управление API токенами
- **Archive**: `/api/v1/archive/*` - Управление архивом
- **Users**: `/api/v1/users/*` - Управление пользователями
- **Health**: `/api/v1/health` - Health check

Полная документация с примерами запросов и ответов: [docs/API.md](docs/API.md)

## 🧪 Тестирование

### Unit тесты

```bash
# Запуск всех unit тестов
pnpm test:unit

# Запуск с покрытием
pnpm test:cov

# Запуск в watch режиме
pnpm test:watch

# Отладка unit тестов
pnpm test:unit:debug
```

### E2E тесты

```bash
# Запуск E2E тестов
pnpm test:e2e

# Отладка E2E тестов
pnpm test:e2e:debug
```

### Все тесты

```bash
pnpm test
```

## 👨‍💻 Разработка

### Полезные команды

```bash
# Backend
pnpm build              # Сборка
pnpm start:dev          # Разработка с hot-reload
pnpm start:debug        # Разработка с отладкой
pnpm start:prod         # Продакшн запуск
pnpm lint               # Линтинг
pnpm format             # Форматирование кода

# Prisma
node prisma-wrapper.mjs migrate dev     # Создание миграции
node prisma-wrapper.mjs migrate deploy  # Применение миграций
node prisma-wrapper.mjs db seed         # Заполнение тестовыми данными
node prisma-wrapper.mjs studio          # Prisma Studio (GUI для БД)
npx prisma generate                     # Генерация клиента

# Frontend
cd ui
pnpm dev                # Разработка
pnpm build              # Сборка
pnpm preview            # Просмотр сборки
```

### Структура модуля (Backend)

Каждый модуль NestJS следует единой структуре:

```
module-name/
├── dto/                    # Data Transfer Objects
│   ├── create-*.dto.ts
│   ├── update-*.dto.ts
│   └── index.ts
├── module-name.controller.ts   # HTTP контроллер
├── module-name.service.ts      # Бизнес-логика
├── module-name.module.ts       # Модуль NestJS
└── index.ts                    # Экспорты
```

### Добавление нового модуля

```bash
# Генерация модуля
nest g module modules/new-module
nest g controller modules/new-module
nest g service modules/new-module

# Создание DTO
mkdir src/modules/new-module/dto
touch src/modules/new-module/dto/create-new-module.dto.ts
touch src/modules/new-module/dto/update-new-module.dto.ts
```

### Работа с базой данных

1. Изменить `prisma/schema.prisma`
2. Создать миграцию: `node prisma-wrapper.mjs migrate dev --name migration-name`
3. Применить в продакшн: `node prisma-wrapper.mjs migrate deploy`
4. Сгенерировать клиент: `npx prisma generate`

### Code Style

- **Backend**: ESLint + Prettier
- **Frontend**: ESLint + Prettier (конфигурация Nuxt)
- **Commits**: Conventional Commits (рекомендуется)

### Отладка

**Backend:**
```bash
# Запуск с отладчиком
pnpm start:debug

# В VS Code добавить конфигурацию:
{
  "type": "node",
  "request": "attach",
  "name": "Attach to NestJS",
  "port": 9229
}
```

**Frontend:**
```bash
# Nuxt DevTools включены по умолчанию
# Открыть в браузере: http://localhost:3000
```

## 📝 Лицензия

MIT

## 👥 Автор

Ivan K

---

**Полезные ссылки:**
- [API документация](docs/API.md)
- [Архитектура проекта](docs/ARCHITECTURE.md)
- [Конфигурация](docs/CONFIGURATION.md)
- [Развертывание](docs/DEPLOYMENT.md)
- [NestJS документация](https://docs.nestjs.com/)
- [Nuxt документация](https://nuxt.com/)
- [Prisma документация](https://www.prisma.io/docs)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
