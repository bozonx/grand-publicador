# Grand Publicador

Платформа управления публикациями в социальных сетях, запускаемая как Telegram Mini App.

## 📋 Описание

Grand Publicador — веб-приложение для управления публикациями в социальных сетях, которое обеспечивает удобный интерфейс для работы с контентом, каналами и командами.

## 🛠 Технологический стек

- **Frontend**: Nuxt 4, Vue 3, TypeScript
- **Styling**: TailwindCSS 4
- **Backend**: Supabase (PostgreSQL + REST API)
- **State Management**: Pinia
- **Forms**: FormKit
- **Editor**: Tiptap
- **i18n**: @nuxtjs/i18n (ru, en)
- **Auth**: Telegram Mini Apps SDK

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+
- pnpm 8+
- Supabase проект

### Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd grand-publicador
```

2. Установите зависимости:
```bash
pnpm install
```

3. Настройте переменные окружения:
```bash
# Скопируйте example файл
cp .env.development.example .env.development

# Отредактируйте .env.development и заполните:
# - SUPABASE_URL
# - SUPABASE_KEY
# - VITE_DEV_TELEGRAM_ID (ваш Telegram ID для разработки)
```

4. Запустите dev сервер:
```bash
pnpm dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 📁 Структура проекта

```
grand-publicador/
├── app/                      # Nuxt 4 app directory
│   ├── components/           # Vue компоненты
│   │   ├── ui/              # UI kit компоненты
│   │   ├── forms/           # Формы
│   │   ├── editor/          # Rich Text Editor
│   │   ├── layout/          # Layout компоненты
│   │   └── features/        # Feature-specific компоненты
│   ├── composables/         # Composable functions
│   ├── layouts/             # Layouts
│   ├── locales/             # i18n переводы (ru, en)
│   ├── middleware/          # Route middleware
│   ├── pages/               # File-based routing
│   ├── plugins/             # Nuxt plugins
│   ├── stores/              # Pinia stores
│   ├── types/               # TypeScript types
│   └── utils/               # Utility functions
├── public/                  # Static assets
├── supabase/                # Supabase migrations & config
├── dev_docs/                # Development documentation
└── nuxt.config.ts           # Nuxt configuration
```

## 🔧 Доступные команды

```bash
# Разработка
pnpm dev              # Запустить dev сервер

# Сборка
pnpm build            # Собрать для production
pnpm preview          # Предпросмотр production сборки

# Проверка кода
pnpm typecheck        # TypeScript проверка
pnpm lint             # ESLint проверка
```

## 📚 Документация

Подробная документация находится в директории `dev_docs/`:
- [PRD.md](dev_docs/PRD.md) - Product Requirements Document

## 🌍 Интернационализация

Приложение поддерживает следующие языки:
- 🇷🇺 Русский (ru) - основной
- 🇬🇧 Английский (en)

Переводы находятся в `app/locales/`

## 🔐 Аутентификация

В production приложение использует Telegram Mini App API для аутентификации.

В development режиме используется mock аутентификация через `VITE_DEV_TELEGRAM_ID` из `.env.development`.

## 📄 Лицензия

См. файл [LICENSE](LICENSE)

## 🤝 Вклад в проект

Этот проект находится в активной разработке. Следите за обновлениями!

---

**Версия:** MVP 1.0.0  
**Дата последнего обновления:** 2025-12-05
