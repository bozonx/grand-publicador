# PRD: Grand Publicador - Платформа управления публикациями в социальных сетях

## Версия документа
- **Версия:** 1.0.0 MVP
- **Дата создания:** 2025-12-05
- **Статус:** Draft

---

## 1. Обзор проекта

### 1.1 Описание
Grand Publicador — веб-приложение для управления публикациями в социальных сетях, которое запускается как Telegram Mini App и обеспечивает удобный интерфейс для работы с контентом, каналами и командами.

### 1.2 Цели MVP
- Создать базовую функциональную платформу для управления публикациями
- Обеспечить удобный интерфейс для работы с постами в различных социальных сетях
- Реализовать систему управления доступом на уровне блогов
- Подготовить основу для дальнейшего расширения функционала

### 1.3 Целевая аудитория
- Контент-менеджеры
- SMM-специалисты
- Блогеры и создатели контента
- Команды, работающие с мультиканальными публикациями

---

## 2. Технологический стек

### 2.1 Frontend Framework
- **Nuxt 4** — современный meta-framework для Vue.js
  - SSR/SSG capabilities
  - Автоимпорт компонентов и composables
  - File-based routing
  - Встроенная оптимизация производительности

### 2.2 UI & Styling
- **TailwindCSS 4** — utility-first CSS framework
  - Современный подход к стилизации
  - Оптимизация размера бандла
  - Кастомизация дизайн-системы
  
- **Nuxt UI** или **Shadcn-vue** — UI компоненты
  - Готовые, доступные компоненты
  - Соответствие modern design patterns
  - Темизация из коробки

### 2.3 Управление формами
**Рекомендуемая библиотека: FormKit**

**Преимущества:**
- Специально разработана для Vue 3 и Nuxt
- Декларативный подход к созданию форм
- Встроенная валидация
- Поддержка i18n
- Генерация схем форм
- Accessibility из коробки
- TypeScript support

**Решение для MVP:** FormKit + Zod для схем валидации

### 2.4 Backend & Database
- **Supabase** — BaaS платформа
  - PostgreSQL база данных
  - Автоматическая генерация REST API
  - Real-time subscriptions
  - Row Level Security (RLS)
  - Встроенная аутентификация
  - Storage для медиа

- **Supabase JS Client** — официальный клиент для работы с API

### 2.5 State Management
- **Pinia** — официальное state management решение для Vue 3
  - TypeScript support
  - Devtools integration
  - Модульная архитектура

### 2.6 Telegram Integration
- **Telegram Mini Apps SDK** (`@telegram-apps/sdk-vue`)
  - Интеграция с Telegram WebApp API
  - Получение user_id и данных пользователя
  - Доступ к функциям Telegram (haptic feedback, theme, etc.)

### 2.7 Интернационализация (i18n)
**Рекомендуемая библиотека: @nuxtjs/i18n**

**Преимущества:**
- Официальный модуль Nuxt
- Lazy loading переводов
- SEO-friendly (hreflang, локализованные URL)
- Автоматическое определение языка браузера
- Интеграция с Vue I18n
- Поддержка плюрализации и форматирования дат/чисел

**Поддерживаемые языки (MVP):**
- Русский (ru) — основной
- Английский (en)

**Структура переводов:**
```
app/
├── locales/
│   ├── ru.json
│   └── en.json
```

### 2.8 Rich Text Editor
**Рекомендуемая библиотека: Tiptap**

**Почему Tiptap:**
- Headless редактор — полный контроль над UI
- Модульная архитектура — подключаем только нужные расширения
- Основан на ProseMirror — надежный и производительный core
- Отличная поддержка Vue 3 / Nuxt
- TypeScript support
- Активное сообщество и развитие
- Бесплатный open-source core

**Основные расширения для MVP:**
- `@tiptap/starter-kit` — базовый набор (bold, italic, lists, headings)
- `@tiptap/extension-link` — ссылки
- `@tiptap/extension-placeholder` — плейсхолдер
- `@tiptap/extension-character-count` — счетчик символов

**Альтернативы:**
- **Quill** — популярный, но менее гибкий
- **Editor.js** — блочный редактор, хорош для статей
- **CKEditor** — мощный, но тяжеловесный и с коммерческой лицензией

### 2.9 Дополнительные библиотеки
- **@nuxtjs/supabase** — Nuxt модуль для Supabase интеграции
- **@nuxtjs/tailwindcss** — интеграция TailwindCSS
- **@nuxtjs/i18n** — интернационализация
- **@tiptap/vue-3** — Rich Text Editor
- **@vueuse/core** — композиция утилит
- **dayjs** — работа с датами
- **zod** — TypeScript-first схемы валидации

### 2.10 Development Tools
- **TypeScript** — строгая типизация
- **ESLint + Prettier** — code quality
- **Vitest** — unit testing
- **Playwright** — E2E testing (для будущих этапов)

---

## 3. Архитектура приложения

### 3.1 Общая структура

```
grand-publicador/
├── app/                           # Nuxt 4 app directory
│   ├── components/                # Vue компоненты
│   │   ├── ui/                    # UI kit компоненты
│   │   ├── forms/                 # Формы
│   │   ├── editor/                # Rich Text Editor компоненты
│   │   ├── layout/                # Layout компоненты
│   │   └── features/              # Feature-specific компоненты
│   ├── composables/               # Composable functions
│   ├── layouts/                   # Layouts
│   ├── locales/                   # i18n переводы
│   │   ├── ru.json                # Русский язык
│   │   └── en.json                # Английский язык
│   ├── middleware/                # Route middleware
│   ├── pages/                     # File-based routing
│   ├── plugins/                   # Nuxt plugins
│   ├── stores/                    # Pinia stores
│   ├── types/                     # TypeScript types
│   └── utils/                     # Utility functions
├── public/                        # Static assets
├── server/                        # Server-side code (если нужно)
├── supabase/                      # Supabase migrations & config
│   ├── migrations/
│   └── seed.sql
├── dev_docs/                      # Development documentation
├── .env.example                   # Example environment variables (committed)
├── .env.development.example       # Example dev environment (committed)
├── .env.development               # Dev environment variables (gitignored)
├── .env.production                # Prod environment variables (gitignored)
├── nuxt.config.ts                 # Nuxt configuration
├── tailwind.config.ts             # Tailwind configuration
├── tsconfig.json                  # TypeScript configuration
└── package.json
```

### 3.2 Слои приложения

1. **Presentation Layer** — Vue компоненты, страницы
2. **Business Logic Layer** — Composables, stores
3. **Data Access Layer** — Supabase client, API calls
4. **Authentication Layer** — Telegram auth integration

---

## 4. Модель данных (Database Schema)

### 4.1 Таблица: users

Хранит информацию о пользователях платформы.

```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  telegram_id BIGINT UNIQUE NOT NULL,
  username VARCHAR(255),
  first_name VARCHAR(255),
  last_name VARCHAR(255),
  is_admin BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_users_telegram_id ON users(telegram_id);
```

### 4.2 Таблица: blogs

Блоги — основные рабочие пространства для управления публикациями.

```sql
CREATE TABLE blogs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,
  description TEXT,
  owner_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_blogs_owner_id ON blogs(owner_id);
```

### 4.3 Таблица: blog_members

Связь пользователей с блогами и их ролями.

```sql
CREATE TYPE blog_role AS ENUM ('owner', 'admin', 'editor', 'viewer');

CREATE TABLE blog_members (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  blog_id UUID NOT NULL REFERENCES blogs(id) ON DELETE CASCADE,
  user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  role blog_role NOT NULL DEFAULT 'viewer',
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  UNIQUE(blog_id, user_id)
);

CREATE INDEX idx_blog_members_blog_id ON blog_members(blog_id);
CREATE INDEX idx_blog_members_user_id ON blog_members(user_id);
```

**Роли и права:**
- **owner** — владелец блога (полный доступ, удаление блога)
- **admin** — администратор (управление участниками, каналами, постами)
- **editor** — редактор (создание и редактирование постов)
- **viewer** — наблюдатель (только просмотр)

### 4.4 Таблица: channels

Каналы социальных сетей, привязанные к блогам.

```sql
CREATE TYPE social_media_enum AS ENUM (
  'telegram',
  'site',
  'vk',
  'youtube',
  'tiktok',
  'x',
  'instagram',
  'facebook'
);

CREATE TABLE channels (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  blog_id UUID NOT NULL REFERENCES blogs(id) ON DELETE CASCADE,
  name VARCHAR(255) NOT NULL,
  social_media social_media_enum NOT NULL,
  channel_identifier VARCHAR(500), -- ID канала, username, URL и т.д.
  credentials JSONB, -- Токены, ключи доступа (зашифрованные)
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_channels_blog_id ON channels(blog_id);
CREATE INDEX idx_channels_social_media ON channels(social_media);
```

### 4.5 Таблица: posts

Публикации, привязанные к каналам.

```sql
CREATE TYPE post_type_enum AS ENUM ('post', 'article', 'news', 'video', 'short');

CREATE TABLE posts (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  channel_id UUID NOT NULL REFERENCES channels(id) ON DELETE CASCADE,
  author_id UUID NOT NULL REFERENCES users(id),
  
  -- Обязательные поля
  content TEXT NOT NULL,
  social_media social_media_enum NOT NULL,
  post_type post_type_enum NOT NULL,
  
  -- Необязательные поля
  title VARCHAR(500),
  description TEXT,
  author_comment TEXT,
  tags TEXT[], -- Массив тегов
  post_date TIMESTAMPTZ, -- Дата из контента (например, дата новости)
  
  -- Метаданные
  status VARCHAR(50) DEFAULT 'draft', -- draft, scheduled, published, failed
  scheduled_at TIMESTAMPTZ,
  published_at TIMESTAMPTZ,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_posts_channel_id ON posts(channel_id);
CREATE INDEX idx_posts_author_id ON posts(author_id);
CREATE INDEX idx_posts_status ON posts(status);
CREATE INDEX idx_posts_created_at ON posts(created_at DESC);
```

### 4.6 Row Level Security (RLS) Policies

Supabase RLS обеспечит безопасность данных на уровне строк.

**Основные политики:**

1. **Users:**
   - SELECT: Все аутентифицированные пользователи могут видеть других
   - UPDATE: Только свой профиль
   - Админы могут управлять всеми

2. **Blogs:**
   - SELECT: Только участники блога
   - INSERT: Любой аутентифицированный пользователь
   - UPDATE/DELETE: Только owner или admin

3. **Blog Members:**
   - SELECT: Участники того же блога
   - INSERT/UPDATE/DELETE: Только owner или admin блога

4. **Channels:**
   - SELECT: Участники блога
   - INSERT/UPDATE/DELETE: Owner, admin, editor блога

5. **Posts:**
   - SELECT: Участники блога
   - INSERT: Editor, admin, owner
   - UPDATE: Автор или admin/owner
   - DELETE: Owner, admin

---

## 5. Функциональные требования

### 5.1 Аутентификация

#### 5.1.1 Telegram Auth
- При запуске приложения из Telegram Mini App получаем `telegram_id` пользователя
- Проверяем наличие пользователя в БД
- Если пользователь новый — создаем запись в таблице `users`
- Если существует — загружаем данные профиля

#### 5.1.2 Dev Mode Auth
- В `.env.development` указывается `VITE_DEV_TELEGRAM_ID`
- При локальной разработке используется этот ID для эмуляции авторизации
- Режим определяется автоматически через `process.env.NODE_ENV`

### 5.2 Управление пользователями (Admin)

**Доступно только для пользователей с `is_admin = true`**

#### Функции:
- Просмотр списка всех пользователей
- Поиск по username, имени
- Назначение/снятие прав администратора
- Блокировка/разблокировка пользователей (опционально для MVP)

### 5.3 Управление блогами

#### 5.3.1 Список блогов
- Отображение всех блогов, где пользователь является участником
- Фильтрация по роли
- Поиск по названию

#### 5.3.2 Создание блога
**Форма:**
- Название (обязательное)
- Описание (опциональное)

**Логика:**
- Создатель автоматически становится owner
- Создается запись в `blog_members` с ролью `owner`

#### 5.3.3 Редактирование блога
**Доступ:** owner, admin

**Поля:**
- Название
- Описание

#### 5.3.4 Удаление блога
**Доступ:** только owner

**Логика:**
- Каскадное удаление всех связанных данных (channels, posts, members)

#### 5.3.5 Управление участниками блога
**Доступ:** owner, admin

**Функции:**
- Приглашение пользователей (по username или telegram_id)
- Назначение ролей (viewer, editor, admin)
- Изменение ролей
- Удаление участников
- Owner не может быть удален или изменена роль (только передача прав)

### 5.4 Управление каналами

#### 5.4.1 Список каналов блога
- Отображение всех каналов текущего блога
- Группировка/фильтрация по типу социальной сети
- Индикатор активности (is_active)

#### 5.4.2 Создание канала
**Доступ:** owner, admin, editor

**Форма:**
- Название канала (обязательное)
- Тип социальной сети (select: telegram, site, vk, youtube, tiktok, x, instagram, facebook)
- Идентификатор канала (username, URL, ID)
- Учетные данные (токены, ключи) — опционально, зашифровано
- Статус активности (checkbox)

#### 5.4.3 Редактирование канала
**Доступ:** owner, admin, editor

**Поля:** те же, что при создании

#### 5.4.4 Удаление канала
**Доступ:** owner, admin

**Логика:**
- Если есть связанные посты, показать предупреждение
- Каскадное удаление или перенос постов (уточнить в будущем)

### 5.5 Управление постами

#### 5.5.1 Список постов
- Отображение всех постов канала
- Фильтрация по:
  - Статусу (draft, scheduled, published, failed)
  - Типу поста (post, article, news, video, short)
  - Дате создания
  - Автору
- Сортировка по дате создания, публикации
- Пагинация

#### 5.5.2 Создание поста
**Доступ:** owner, admin, editor

**Форма — обязательные поля:**
- Content (textarea/rich editor) — тело поста
- Social Media (auto-filled from channel или select)
- Post Type (select: post, article, news, video, short)

**Форма — необязательные поля:**
- Title (text input, max 500 chars)
- Description (textarea)
- Author Comment (textarea) — комментарий автора
- Tags (multi-input или tag selector)
- Post Date (datepicker) — дата из контента, не дата публикации

**Дополнительные поля:**
- Status (select: draft, scheduled)
- Scheduled At (datetime picker) — если статус scheduled

**Логика:**
- По умолчанию статус = draft
- Автор = текущий пользователь
- Social media подтягивается из выбранного канала

#### 5.5.3 Редактирование поста
**Доступ:** автор, admin, owner

**Поля:** те же, что при создании

**Дополнительная логика:**
- Если пост уже опубликован, показать предупреждение
- История изменений (опционально для будущих версий)

#### 5.5.4 Удаление поста
**Доступ:** автор, admin, owner

#### 5.5.5 Публикация поста
**Логика (для будущих версий):**
- Интеграция с API социальных сетей
- Отправка контента в соответствующий канал
- Обновление статуса и времени публикации

### 5.6 Настройки пользователя

#### Функции:
- Просмотр профиля (данные из Telegram)
- Изменение отображаемого имени (опционально)
- Настройки уведомлений (для будущих версий)
- Настройки интерфейса (тема, язык) — опционально

---

## 6. Пользовательский интерфейс

### 6.1 Принципы дизайна

**MVP фокус:**
- 🎯 **Функциональность > Эстетика** — приоритет на удобство работы
- 🧩 **Консистентность** — единый язык дизайна
- ♿ **Доступность** — следование WCAG рекомендациям
- 📱 **Mobile-first** — оптимизация под мобильные устройства (Telegram)
- ⚡ **Производительность** — быстрая загрузка и отклик

**Визуальный стиль:**
- Сдержанная цветовая палитра (2-3 основных цвета)
- Четкая типографика (системные шрифты или Inter/Roboto)
- Минимум декоративных элементов
- Фокус на читаемости и структуре контента
- Соответствие Telegram Design Guidelines для Mini Apps

### 6.2 Структура навигации

```
├── Home / Dashboard
│   └── Быстрый доступ к блогам, недавние посты
├── Blogs
│   ├── List of Blogs
│   └── Blog Detail
│       ├── Channels
│       ├── Posts
│       └── Members (admin only)
├── Users (admin only)
│   └── User Management
└── Settings
    └── User Settings
```

### 6.3 Основные экраны

#### 6.3.1 Dashboard (Главная)
- Список блогов пользователя с индикаторами
- Последние созданные/измененные посты
- Быстрые действия (создать пост, создать блог)

#### 6.3.2 Blog Detail
**Tabs / Sections:**
1. **Overview** — информация о блоге
2. **Channels** — список каналов с кнопкой добавления
3. **Posts** — список постов всех каналов блога
4. **Members** — управление участниками (для admin/owner)

#### 6.3.3 Post Form (Create/Edit)
- Шапка с названием и кнопками действий (Save Draft, Schedule, Publish)
- Основные поля группой
- Дополнительные поля в collapsible секции или отдельной вкладке
- Preview поста (опционально)

#### 6.3.4 Channel Form (Create/Edit)
- Компактная форма или модальное окно
- Группировка полей по смыслу

#### 6.3.5 User Management (Admin)
- Таблица пользователей с фильтрами
- Действия: назначить админа, заблокировать (будущее)

### 6.4 UI Components (примерный набор)

**Базовые:**
- Button (primary, secondary, ghost, danger)
- Input (text, textarea, number, date, datetime)
- Select / Dropdown
- Checkbox / Radio
- Badge / Chip (для тегов, статусов)
- Avatar
- Card
- Modal / Dialog
- Toast / Notification
- Tabs
- Table with pagination

**Сложные:**
- Form Builder (FormKit)
- Rich Text Editor (Tiptap) — для content поля
- Date/Time Picker
- Multi-select Tags Input
- Confirmation Dialogs
- Language Switcher — переключение языка интерфейса

---

## 7. Нефункциональные требования

### 7.1 Производительность
- Time to Interactive (TTI) < 3 секунды
- Оптимизация бандлов через code splitting
- Lazy loading компонентов и страниц
- Кеширование данных из Supabase

### 7.2 Безопасность
- Все запросы через HTTPS
- Валидация данных на клиенте и на уровне БД
- RLS политики в Supabase
- Хранение чувствительных данных (tokens) в зашифрованном виде
- CORS настройки для ограничения доступа

### 7.3 Масштабируемость
- Модульная архитектура для легкого расширения
- Переиспользуемые компоненты
- Разделение бизнес-логики и представления

### 7.4 Доступность (A11y)
- Семантический HTML
- ARIA attributes где необходимо
- Keyboard navigation
- Screen reader support
- Контрастность цветов (WCAG AA минимум)

### 7.5 Совместимость
- Telegram Mini Apps (iOS, Android, Desktop)
- Современные браузеры (Chrome, Safari, Firefox последних версий)

---

## 8. Технические требования

### 8.1 Переменные окружения

#### `.env.example` (коммитится в репозиторий)
```env
# ===========================================
# EXAMPLE ENVIRONMENT VARIABLES
# Copy this file to .env.development or .env.production
# and fill in the actual values
# ===========================================

# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key

# Development Mode
# Set to true for local development, false for production
VITE_DEV_MODE=false

# Dev Telegram User ID (only for development)
# Used to simulate Telegram auth in local dev environment
VITE_DEV_TELEGRAM_ID=

# App Configuration
VITE_APP_NAME=Grand Publicador

# Default locale (ru or en)
VITE_DEFAULT_LOCALE=ru
```

#### `.env.development.example` (коммитится в репозиторий)
```env
# ===========================================
# DEVELOPMENT ENVIRONMENT VARIABLES
# Copy this file to .env.development and fill in actual values
# ===========================================

# Supabase Configuration (use development project)
SUPABASE_URL=https://your-dev-project.supabase.co
SUPABASE_KEY=your-dev-anon-key

# Development Mode - enables mock auth and debug features
VITE_DEV_MODE=true

# Your Telegram User ID for testing
# Get it from @userinfobot in Telegram
VITE_DEV_TELEGRAM_ID=123456789

# App Configuration
VITE_APP_NAME=Grand Publicador (Dev)

# Default locale for development
VITE_DEFAULT_LOCALE=ru
```

#### `.env.development` (НЕ коммитится, в .gitignore)
```env
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key

# Development Mode
VITE_DEV_MODE=true
VITE_DEV_TELEGRAM_ID=123456789

# App Config
VITE_APP_NAME=Grand Publicador (Dev)
VITE_DEFAULT_LOCALE=ru
```

#### `.env.production` (НЕ коммитится, в .gitignore)
```env
# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key

# Development Mode
VITE_DEV_MODE=false

# App Config
VITE_APP_NAME=Grand Publicador
VITE_DEFAULT_LOCALE=ru
```

### 8.2 Конфигурация Nuxt

```typescript
// nuxt.config.ts
export default defineNuxtConfig({
  modules: [
    '@nuxtjs/supabase',
    '@nuxtjs/tailwindcss',
    '@nuxtjs/i18n',
    '@pinia/nuxt',
    '@vueuse/nuxt',
    '@formkit/nuxt',
  ],
  
  supabase: {
    redirect: false, // Отключаем редирект, т.к. auth через Telegram
  },
  
  i18n: {
    locales: [
      { code: 'ru', name: 'Русский', file: 'ru.json' },
      { code: 'en', name: 'English', file: 'en.json' },
    ],
    defaultLocale: 'ru',
    lazy: true,
    langDir: 'locales/',
    strategy: 'no_prefix', // URL без префикса языка для Mini App
    detectBrowserLanguage: {
      useCookie: true,
      cookieKey: 'i18n_locale',
      fallbackLocale: 'ru',
    },
  },
  
  typescript: {
    strict: true,
    typeCheck: true,
  },
  
  app: {
    head: {
      title: 'Grand Publicador',
      viewport: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no',
      meta: [
        { name: 'theme-color', content: '#000000' },
      ],
    },
  },
  
  experimental: {
    viewTransition: true, // Smooth transitions
  },
})
```

### 8.3 База данных

**Версионирование:**
- Все изменения схемы через Supabase migrations
- Миграции хранятся в `supabase/migrations/`
- Seed данные для dev в `supabase/seed.sql`

**Индексы:**
- Создать индексы для всех внешних ключей
- Индексы для часто используемых фильтров (status, created_at, social_media)

### 8.4 TypeScript типы

Генерация типов из Supabase схемы:
```bash
npx supabase gen types typescript --project-id your-project-id > app/types/database.ts
```

---

## 9. MVP Scope & Roadmap

### 9.1 MVP v1.0 (Текущий этап)

**Цель:** Базовая функциональность для работы с публикациями

**Включено:**
✅ Аутентификация через Telegram  
✅ CRUD для Users (admin only)  
✅ CRUD для Blogs  
✅ Управление участниками блога (roles)  
✅ CRUD для Channels  
✅ CRUD для Posts  
✅ Формы создания/редактирования всех сущностей  
✅ Базовые настройки пользователя  
✅ RLS политики в Supabase  
✅ Базовый UI kit  

**Не включено в MVP:**
❌ Публикация постов в соц. сети (только хранение)  
❌ Планировщик публикаций  
❌ Медиа-библиотека  
❌ Rich text editor  
❌ Аналитика и статистика  
❌ История изменений постов  
❌ Уведомления  

### 9.2 Будущие этапы

#### v1.1 — Медиа и редактор
- Интеграция Supabase Storage для медиа
- Rich text editor для content
- Превью постов
- Оптимизация изображений

#### v1.2 — Публикация
- Интеграция с API социальных сетей
- Планировщик публикаций
- Статусы публикации (pending, published, failed)
- Retry механизм

#### v1.3 — Аналитика
- Статистика постов
- Метрики по каналам
- Dashboard с аналитикой

#### v2.0 — Дополнительные функции
- Шаблоны постов
- Массовые операции
- Импорт/экспорт данных
- Webhook интеграции
- AI-ассистент для контента

---

## 10. Риски и ограничения

### 10.1 Технические риски

| Риск | Вероятность | Влияние | Митигация |
|------|-------------|---------|-----------|
| Проблемы с Telegram Mini App API | Средняя | Высокое | Тщательное тестирование, fallback сценарии |
| Ограничения Supabase Free Tier | Средняя | Среднее | Мониторинг использования, план миграции |
| Производительность на слабых устройствах | Низкая | Среднее | Code splitting, оптимизация бандла |

### 10.2 Ограничения MVP

- Нет реальной публикации в соц. сети (только подготовка контента)
- Минимальный функционал для управления медиа
- Нет системы уведомлений
- Базовый текстовый редактор без rich formatting

---

## 11. Метрики успеха MVP

### 11.1 Технические метрики
- ✅ 100% функциональности из MVP scope реализовано
- ✅ 0 критичных багов на production
- ✅ TTI < 3 секунды на 4G соединении
- ✅ Lighthouse score > 80 (Performance, Accessibility)

### 11.2 Пользовательские метрики
- ✅ Возможность создать блог, канал и пост за < 2 минут
- ✅ Интуитивная навигация без обучения
- ✅ Отсутствие блокирующих ошибок при базовых операциях

---

## 12. Развертывание

### 12.1 Хостинг Frontend
**Рекомендуется:** Vercel или Netlify

**Преимущества:**
- Автоматический deploy из Git
- Preview deployments для PR
- Edge network для быстрой загрузки
- Автоматическая настройка для Nuxt

### 12.2 Хостинг Backend
**Supabase Cloud:**
- Управляемая PostgreSQL БД
- Автоматические бэкапы
- Встроенный CDN для storage

### 12.3 CI/CD
- GitHub Actions для автоматического тестирования
- Автоматический deploy на Vercel при merge в main
- Preview environments для feature branches

---

## 13. Дальнейшие шаги

### Этап 1: Подготовка (1-2 дня)
- [ ] Создать Supabase проект
- [ ] Настроить репозиторий
- [ ] Scaffolding Nuxt 4 проекта
- [ ] Установка зависимостей
- [ ] Настройка ESLint, Prettier

### Этап 2: База данных (2-3 дня)
- [ ] Создать миграции для всех таблиц
- [ ] Настроить RLS политики
- [ ] Создать seed данные для тестирования
- [ ] Генерация TypeScript типов

### Этап 3: Аутентификация (2-3 дня)
- [ ] Интеграция Telegram Mini App SDK
- [ ] Composable для auth
- [ ] Dev mode с mock user_id
- [ ] Middleware для защиты роутов

### Этап 4: UI Foundation (3-4 дня)
- [ ] Настройка Tailwind + UI библиотека
- [ ] Создание базовых компонентов
- [ ] Layouts (main, auth, admin)
- [ ] Навигация

### Этап 5: Core Features (10-14 дней)
- [ ] Blogs CRUD
- [ ] Blog members management
- [ ] Channels CRUD
- [ ] Posts CRUD
- [ ] Forms с FormKit
- [ ] User management (admin)

### Этап 6: Тестирование и Оптимизация (3-5 дней)
- [ ] Тестирование всех функций
- [ ] Исправление багов
- [ ] Оптимизация производительности
- [ ] Accessibility audit

### Этап 7: Deploy (1-2 дня)
- [ ] Настройка production окружения
- [ ] Deploy на Vercel
- [ ] Проверка в Telegram
- [ ] Документация для пользователей

---

## 14. Заключение

Данный PRD описывает MVP версию платформы Grand Publicador для управления публикациями в социальных сетях. Проект построен на современном стеке технологий (Nuxt 4, Supabase, TailwindCSS), обеспечивающем высокую производительность, масштабируемость и удобство разработки.

MVP фокусируется на базовом функционале управления контентом без реальной публикации в соц. сети, что позволяет быстро оценить работоспособность концепции и получить обратную связь для дальнейшего развития.

## Приложения

### A. Глоссарий

- **Blog** — рабочее пространство для управления публикациями
- **Channel** — канал социальной сети, привязанный к блогу
- **Post** — публикация/пост для размещения в канале
- **Blog Member** — участник блога с определенной ролью
- **RLS** — Row Level Security, механизм безопасности на уровне строк в БД
- **Mini App** — веб-приложение, запускаемое внутри Telegram

### B. Ссылки

- [Nuxt 4 Documentation](https://nuxt.com)
- [Supabase Documentation](https://supabase.com/docs)
- [FormKit Documentation](https://formkit.com)
- [Tiptap Editor](https://tiptap.dev)
- [Nuxt I18n](https://i18n.nuxtjs.org)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [TailwindCSS](https://tailwindcss.com)

---

**Дата последнего обновления:** 2025-12-05  
**Версия:** 1.0.0 MVP
